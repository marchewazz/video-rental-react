import strings from "../utilities/strings";

export default function MainPage() {

    return (
        <div>
            <p>
                { strings.mainPage.title }
            </p>
        </div>
    )
}